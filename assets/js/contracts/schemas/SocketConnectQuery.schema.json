{
  "type": "object",
  "description": "Query parameters used by the Phoenix JS client during socket connect",
  "properties": {
    "vsn": {
      "type": "string",
      "const": "2.0.0",
      "description": "Phoenix serializer version added by the Phoenix client"
    },
    "user_token": {
      "type": "string",
      "description": "Signed user token issued on page render and accepted during socket authentication with a maximum age of 86400 seconds\n"
    }
  },
  "required": ["vsn", "user_token"],
  "additionalProperties": false
}
